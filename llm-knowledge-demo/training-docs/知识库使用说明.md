# 知识库使用说明

本演示把培训资料保存在服务器端的 training-docs 目录。网页打开时只加载资料目录，不会一次性下载全部正文。

用户点击某份资料时，浏览器才通过 /api/docs/:id 加载这份资料正文。用户提问时，浏览器把问题发送给 /api/ask，由服务器检索相关资料片段。

如果启用了本地 Ollama，服务器会把检索到的资料片段和问题一起发送给本地大语言模型。API Key 不会出现在浏览器里，内部资料也不需要上传到第三方模型服务。

如果没有启用 Ollama，系统会返回一个基于资料片段的演示答案，方便验证知识库检索链路是否正常。
